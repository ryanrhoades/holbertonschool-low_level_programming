#include "main.h"
/**
 * _strcpy I hope I'm doing this right
 * @dest: don't make me do this again
 * @src: Why is it still hard
 * Return: with good grades
 */
char *_strcpy(char *dest, char *src)
{
	return (0);
}
